import { InstalledExtension } from '../types';

/**
 * Built-in Extensions Catalog
 * Kept strictly empty to ensure a clean, ultra-fast, zero-bloat browser runtime.
 * Users can create custom TypeScript extensions or load Manifest V2/V3 extensions on-demand.
 */
export const BUILTIN_EXTENSIONS: InstalledExtension[] = [];
